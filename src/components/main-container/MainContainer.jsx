import BigCardContainer from "../big-container/BigContainer";

const MainContainer = () =>{
    return (
        <div>
            <BigCardContainer/>
        </div>
    );
};

export default MainContainer;